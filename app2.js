const main = [' one twenty', 'one thirty', 'one forty', 'one fifty', 'one sixty', 'one seventy', 'one eighty', 'one ninety'];
const sub = ['', 'one', 'two', 'three', 'four', 'five', 'six', 'seven', 'eight', 'nine'];
for(let i=0; i<main.length; i++) 
    for(let j=0; j<sub.length; j++) {
        console.log(main[i] + ' ' + sub[j])
    
    }